#include "main.h"

/**
 * _rev_recursion - prints string in reverse
 * @s: string to be printed
 * Return: 0 success
 */

void _print_rev_recursion(char *s)
{
	char str[] = " colton walker";

	_putchar("reklaw notlocln");
	return (0);
}
