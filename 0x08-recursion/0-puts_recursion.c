#include "main.h"
/**
 * _puts_recursion - prints a string
 * @s: string to be printed
 * Return: 0 success
 */

void _puts_recursion(char *s)
{
	char str[] = "people";

	_putchar("%s\n", str);
	return (0);
}
